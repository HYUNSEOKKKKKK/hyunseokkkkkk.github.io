# Project pages

This directory is reserved for dedicated project pages.

A future project page can live at `projects/project-slug/index.html` and reuse the shared stylesheet at `../../assets/css/styles.css`. Add the project URL to the matching resource button in `index.html` only when the page exists.
